# Copy to .env and customize before running locally or in CI

# --- Database Configuration ---
DB_HOST=localhost
DB_NAME=ecommerce
DB_USER=postgres
DB_PASSWORD=password
DB_PORT=5432
DB_POOL_MAX=10

# --- External Services ---
PRODUCT_SERVICE_URL=http://localhost:8000

# --- Server ---
PORT=8001

# --- AWS X-Ray ---
AWS_XRAY_DAEMON_ADDRESS=localhost:2000
AWS_XRAY_TRACING_NAME=OrderService
